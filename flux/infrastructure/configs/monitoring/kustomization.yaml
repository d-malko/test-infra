apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - alertmanagerconfig.yaml
  - grafana-httproute.yaml
  # otel-collector-httproute.yaml — deferred to IF-66 (requires Cloudflare Access auth first)
