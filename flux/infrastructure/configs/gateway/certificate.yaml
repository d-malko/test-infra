apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-p2bid-global-tls
  namespace: gateway
spec:
  secretName: wildcard-p2bid-global-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - "*.p2bid.global"
