apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-vars
  namespace: flux-system
data:
  # GCP project that holds secrets in Secret Manager.
  # Set this to your actual GCP project ID.
  gcp_project_id: "test-staging-xc69rp"
