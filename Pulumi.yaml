name: test-infra
runtime:
  name: python
  options:
    virtualenv: venv
description: test infrastructure – Flux bootstrap via Pulumi
backend:
  # GCS bucket for Pulumi state. Create once:
  #   gcloud storage buckets create gs://test-infra-state \
  #     --location=europe-west1 --uniform-bucket-level-access
  url: gs://test-staging-xc69rp-infra-state
